# ===========================================================================
# PRODUCTION environment SAMPLE.
#
# Copy this file to `.env.prod` and fill in REAL secret values:
#     cp .env.prod.sample.txt .env.prod
#
# Never commit the real `.env.prod` to version control.
# Media files are stored on AWS S3 in production (see config/settings/prod.py).
# ===========================================================================

# --- Django ---
DJANGO_SETTINGS_MODULE=config.settings.prod
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
DJANGO_SECRET_KEY=REPLACE_WITH_A_LONG_RANDOM_SECRET
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=renter.example.com,www.renter.example.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://renter.example.com,https://www.renter.example.com
DJANGO_TIME_ZONE=UTC
DJANGO_LOG_LEVEL=INFO
DJANGO_SECURE_SSL_REDIRECT=True
DJANGO_SECURE_HSTS_SECONDS=2592000

# --- Database (PostgreSQL) ---
# Point HOST at the in-compose `postgres` service, or your managed DB (e.g. RDS).
POSTGRES_DB=renter
POSTGRES_USER=renter_prod
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_CONN_MAX_AGE=60

# --- Redis / Celery ---
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=django-db
CELERY_WORKER_CONCURRENCY=4

# --- JWT ---
JWT_ACCESS_TOKEN_LIFETIME_MIN=15
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# --- AWS S3 (media storage) ---
AWS_ACCESS_KEY_ID=REPLACE_WITH_AWS_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=REPLACE_WITH_AWS_SECRET_KEY
AWS_STORAGE_BUCKET_NAME=renter-prod-media
AWS_S3_REGION_NAME=us-east-1
# Optional CloudFront / custom CDN domain (leave blank to use the S3 URL):
AWS_S3_CUSTOM_DOMAIN=
AWS_QUERYSTRING_AUTH=False

# --- Email (SMTP) ---
DJANGO_DEFAULT_FROM_EMAIL=no-reply@renter.example.com
LISTING_MODERATION_EMAIL=moderation@renter.example.com
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=REPLACE_WITH_SMTP_USER
EMAIL_HOST_PASSWORD=REPLACE_WITH_SMTP_PASSWORD
EMAIL_USE_TLS=True

# --- CORS ---
CORS_ALLOWED_ORIGINS=https://app.renter.example.com

# --- Gunicorn ---
GUNICORN_WORKERS=3
GUNICORN_TIMEOUT=60

# --- Sentry (optional) ---
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
