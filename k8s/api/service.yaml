apiVersion: v1
kind: Service

metadata:
  name: renter-api
  namespace: rental

spec:
  selector:
    app: renter
    component: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
    
  type: ClusterIP